# If Homebrew is installed no need for sudo, otherwise sudo must be used
# Hammerspoon config should only be used for ISO layout if "~" key needs to be moves up next to the "1" key like in ANSI

``` sudo curl -fsSL https://raw.githubusercontent.com/PetarSyarov/macos-workstation-setup/main/bootstrap.sh -o /tmp/bootstrap.sh && bash /tmp/bootstrap.sh ```
